import { expect } from 'chai';
import { transitionState, dataModel } from './model'

describe('transitionState, dataModel', () => {
  it('Should select existing DOM element', () => {
    expect(true).to.be.true;
  });
});